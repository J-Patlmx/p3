/*TODO
sass reele utilite ? mieux en css pur avec le recul ? 
modifier l'annimation de checking pour que le check soit automatiser au hover  ) ? dont givup the fight
finir les mediaQueries 
voir avec Jaber si la decoupe du scss est correct(jeudi)
mixin ou pas ? 

  */